import * as React from 'react';
import callbackSvg from './callback.svg';

const Callback = () => {
   return (
    <img src={callbackSvg} className="App-logo" alt="logo" />
  );
};

export default Callback;
